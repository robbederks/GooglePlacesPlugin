This is a plugin for a SiriProxy to use Google Places.

To install:

Add this to your config.yml inside of the .siriproxy foler:

    - name: 'GPP'
      git: 'git://github.com/robbederks/GooglePlacesPlugin.git'
	
These are the supported commands: 'nearby (*)', 'Where am I'

*** NOT FINISHED YET, MAYBE SOME BUGS ***